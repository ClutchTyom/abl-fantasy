-- Каждая запись teams соответствует ровно одной команде в ровно одном
-- дивизионе ABL (см. историю с "AIR STAR TEAM" — один клуб может иметь
-- разные team-записи в разных дивизионах). Сохраняем название дивизиона,
-- чтобы на /players можно было сначала выбрать дивизион, а потом команду.
alter table teams add column if not exists division text;
